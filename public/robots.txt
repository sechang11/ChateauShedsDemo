# This is an unaffiliated demo build, not the Chateau Sheds website.
# It carries the real company's name, addresses and phone number, so it must
# not be indexed alongside — or instead of — the real site.
User-agent: *
Disallow: /
